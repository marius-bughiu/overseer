{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only capabilities (biometric authentication).",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": ["biometric:default"]
}
